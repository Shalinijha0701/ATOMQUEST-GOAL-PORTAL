# AtomQuest Goal Setting & Tracking Portal

Full-stack implementation for the AtomQuest Hackathon 1.0 BRD: employee goal creation, L1 approval, shared KPIs, quarterly achievement updates, manager check-ins, admin governance, audit trail, reporting, and analytics.

## Quick Start

```bash
npm install
npm run dev
```

Client: `http://localhost:3000`  
API: `http://localhost:5000/api`

Copy `.env.example` to `.env` when you want custom settings. The app defaults to `DEMO_MODE=true` so judges can exercise all workflows without waiting for calendar windows. Set `ENFORCE_WINDOWS=true` and `DEMO_MODE=false` to enforce the BRD quarterly dates strictly.

## Vercel Deployment

This project is now configured for Vercel static deployment of the React client with a serverless `/api` backend.

- The static site is built from `client/package.json`.
- API requests under `/api/*` are routed to a Vercel Node serverless handler in `api/[...slug].js`.
- On Vercel, backend data is stored in temporary runtime storage (`/tmp`) and is intended for demo purposes.

To deploy:

1. Push the repo to GitHub.
2. Create a new Vercel project and connect it to this repository.
3. In Vercel settings, no root change is needed because `vercel.json` already points to the `client` build.
4. Optionally add environment variables via Vercel if you need custom settings.

## Demo Logins

| Role | Email | Password |
| --- | --- | --- |
| Employee | employee@atomquest.local | Employee@123 |
| Submitted Employee | submitted@atomquest.local | Employee@123 |
| Approved Employee | approved@atomquest.local | Employee@123 |
| Manager L1 | manager@atomquest.local | Manager@123 |
| Admin / HR | admin@atomquest.local | Admin@123 |

## BRD Coverage

- Phase 1 goal sheets with thrust area, title, description, UoM, target, and weightage.
- Validation: total weightage must equal 100%, minimum goal weightage is 10%, maximum 8 goals.
- L1 manager workflow with inline edits, return for rework, approval, and locking.
- Shared departmental KPIs pushed by manager/admin, with read-only title/target and recipient weightage adjustment.
- Quarterly achievement updates with status and computed progress scores.
- Manager check-in comments and completion dashboard.
- Admin cycle management, unlock exceptions, audit trail, and escalation run.
- CSV achievement report export.
- Analytics for trends, distribution, and manager effectiveness.

## Project Layout

```text
server/   Express API, JSON persistence, auth, audit, reports
client/   Vite React role-based portal
docs/     Architecture and BRD notes
outputs/  User-facing deliverables generated by Codex
```

## Project Description

AtomQuest Goal Portal is a full-stack employee performance and goal-tracking application built for an internal HR/manager workflow. It includes role-based dashboards for employees, managers, and admins, with goal creation, review/approval flows, achievement updates, analytics, CSV reporting, and audit trail support.

## Architecture

- Frontend: React + Vite with a lightweight component structure and role-based page rendering.
- Backend: Express server with REST API endpoints under `/api/*` and JWT-based authentication.
- Data Persistence: JSON file store seeded on first run for demo/demo-only persistence.
- Deployment: Monorepo structure with a client app and server app in the same repository. Vercel serves the built React app as static assets and routes `/api/*` requests to a Node serverless function.

## Vercel Deployment Notes

- The root `vercel.json` now uses a valid Vercel schema version `2`.
- Static assets are built from the root workspace via `package.json` and deployed from `client/dist`.
- API routes use `api/[...slug].js` to load the Express app for serverless handling.
- If the deployment returns `404 NOT_FOUND`, redeploy after pushing the latest configuration.

## How to Run Locally

```bash
npm install
npm run dev
```

- Client: `http://localhost:3000`
- API: `http://localhost:5000/api`

## Deployment Steps

1. Push the latest code to `main`.
2. In Vercel, trigger a redeploy for the project.
3. Verify the production URL after the build completes.
4. If you want to use a custom domain, add it in Vercel and ensure the domain is verified.

